// Generated by CoffeeScript 1.6.2
(function() {
  d3.csv('charts/treemap/data/devices.csv', function(data) {
    var chart, pack, tree, _i, _ref, _results;

    data = data.map(function(d) {
      d.wurfl_device_id = d.wurfl_device_id;
      d.wurfl_fall_back = d.wurfl_fall_back;
      d.brand_name = d.brand_name;
      d.model_name = d.model_name;
      d.visits = +d.visits;
      d.subscribers = +d.subscribers;
      d.conv = +d.conv;
      d.children = [];
      return d;
    });
    tree = {};
    console.log(data.length);
    pack = function(root) {
      return data.forEach(function(d, i) {
        if (d !== null && d.wurfl_fall_back === root.wurfl_device_id) {
          pack(d);
          root.children.push(d);
          return data[i] = null;
        }
      });
    };
    (function() {
      _results = [];
      for (var _i = 0, _ref = data.length; 0 <= _ref ? _i <= _ref : _i >= _ref; 0 <= _ref ? _i++ : _i--){ _results.push(_i); }
      return _results;
    }).apply(this).forEach(function(i) {
      var d;

      d = data[i];
      if (!!d) {
        return pack(data[i]);
      }
    });
    data = data.filter(function(d) {
      return d !== null;
    });
    window.data = data;
    tree = {
      children: data,
      wurfl_device_id: 'root',
      brand_name: 'root',
      model_name: 'root'
    };
    chart = treeMapChart();
    d3.select('#chart').call(chart);
    return chart.draw(tree);
  });

}).call(this);
